import React, { useContext } from 'react';
import { UserContext } from './AuthProvider';

const Home = () => {
    return (
        <div>
            <h2>This is Home </h2>
        </div>
    );
};

export default Home;